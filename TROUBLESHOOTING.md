# つまずきメモ — 作者が実際にハマった穴の一覧

Apple Silicon Macで画像生成を回すと、Windows+NVIDIAの記事には載っていない詰まり方をします。
ここは作者が実際に踏んだ穴と、その正体・対策をまとめたものです。**症状から引いてください。**

---

## 真っ黒な画像しか出てこない

原因が**3種類**あります。作者は3つとも順番に踏みました。上から順に疑ってください。

### ① VAEがfp16で壊れている（いちばん多い）

**症状**: 生成は正常に完走するのに、出てくる画像が真っ黒。エラーは一切出ない。

**正体**: SDXLの標準VAE（絵を最終的なピクセルに戻す部品）には、半精度（fp16）で計算するとNaN——数値が壊れる——という有名な不具合があります。壊れた数値は真っ黒として保存されます。

**対策**: 修正版VAE（`sdxl-vae-fp16-fix`）に差し替える。**本アプリでは対策済み**です。自分で別のパイプラインを組むときは、ここを最初に疑ってください。

### ② clip_skipを二重に掛けている

**症状**: 同上。あるいは絵が破綻する。

**正体**: Stable Diffusion WebUI（Automatic1111）の記事には「アニメ系はclip_skip 2」と必ず書いてあります。これを真に受けてdiffusersに同じ指定をすると、**二重に掛かります**。diffusersのSDXLパイプラインは、最初からclip_skip 2相当の実装になっているためです。

**対策**: **diffusersを使うならclip_skipは渡さない。** これだけです。WebUIの設定値をそのまま持ち込まないでください。

### ③ 拡大用のGANモデルがGPUに居座っている

**症状**: 通常生成は無事なのに、拡大（hires fix）を有効にした途端に黒くなる。バッチの後半だけ黒い。

**正体**: 拡大に使うGANモデル（Real-ESRGAN系）をGPU上に置きっぱなしにすると、メモリを圧迫して他の計算がNaNに落ちます。Macは全部のメモリを共有しているので、この影響を受けやすい。

**対策**: 使う瞬間だけGPUに載せて、直後にCPUへ降ろしてキャッシュを開放する。**本アプリでは対策済み**です。

> **補足**: 上の3つに当てはまらないのに黒い場合は、**巨大なモデルを並行ダウンロード中**などでメモリが逼迫していないか確認してください。メモリが厳しい状況では、それだけでNaNが出ます。

---

## 生成が異常に遅い（想定の5倍かかる）

**症状**: いつもは5秒/itなのに25秒/itしか出ない。20枚バッチが6時間コースになる。

**正体**: **他のアプリがGPUを占有しています。** 作者の場合、起動しっぱなしにしていたDraw Thingsが犯人でした。img-forgeを止めた状態でもGPU使用率が99%、メモリを18.8GB確保していました。

**対策**: img-forgeを止めた状態で、以下を実行してGPUが空いているか確認します。

```bash
ioreg -r -d 1 -w 0 -c IOAccelerator | grep -o '"Device Utilization %"=[0-9]*'
```

ここが高ければ、外部に犯人がいます。Draw Things / ComfyUI / 動画生成ツールなど、GPUを使うアプリを終了してください。犯人を落としたら1%に戻り、速度も即座に回復します。

**10枚以上のバッチを走らせる前に、これを1回見る習慣をつけると幸せになれます。**

---

## モデルがダウンロードできない

### Civitaiから直接落とそうとして失敗する

**症状**: ダウンロードURLを叩くとログイン画面にリダイレクトされる（curlならexit 56）。

**正体**: Civitaiのダウンロードは認証必須です。

**対策**: 2つあります。

1. **Hugging Faceのミラーを探す**（おすすめ）。同じモデルがdiffusers形式で置かれていることが多いです。`https://huggingface.co/api/models?search=<モデル名>` で検索できます。
2. Civitaiでアカウントを作り、APIキーを発行して環境変数に入れる。
   ```bash
   export CIVITAI_TOKEN="あなたのキー"
   ```

### ダウンロード中に「xet」系のエラーで落ちる

**症状**: Hugging Faceからのダウンロード中に `CAS Client Error` で失敗する。

**対策**: 新しい転送方式を切って、旧方式で再試行します。

```bash
HF_HUB_DISABLE_XET=1 .venv/bin/python fetch_model.py ...
```

---

## 絵が灰色に壊れる／ノイズのまま出てくる

**正体**: **V-prediction（v-pred）版のモデルを、通常版として動かしています。**

人気モデルには「通常版」と「Vpred版」の兄弟がいることがあり（NoobAI-XLなど）、この2つは内部の計算方式が違います。取り違えると、絵にならない灰色が出ます。

**対策**: モデルの配布ページで予測方式を確認し、`models.json` の `vpred` を正しく設定してください。ファイル名に `vpred` と入っていることが多いですが、入っていない場合もあるので配布ページが正です。

---

## プロンプトの後半が効いていない気がする

**正体**: **77トークンの制限で、後半が黙って切り捨てられています。**

diffusersを直接使う構成には、標準では長いプロンプトを分割処理する機能がありません。作者は84トークンのプロンプトを書いて、後半の `upper body` などが無言で消えていたことに後から気づきました。

**対策**:

- 本文タグを詰め込みすぎない
- **重要なタグほど前に置く**
- 実行時のログに出る CLIP truncation の警告を見る癖をつける

---

## LoRAが効いているのか分からない

**正体**: **ロードが成功することと、効いていることは別です。** エラーが出ないのに絵が全く変わらないことがあります。

また、ファイル名や内部の命名規則から互換性を判定するのは**当てになりません**。作者は「この命名は読めない」と一度結論を出して、後から実測で覆しました。世代が違うはずのSD1.5用LoRAがSDXLで普通に効いた例もあります。

**対策**: **同じプロンプト・同じシードで、LoRAあり／なしを1枚ずつ焼いて見比べる。** これが唯一の判定方法です。目視で分からなければ、平均差分が5/255を超えていれば「効いている」と判断できます。

---

## 肌がゴム手袋みたいにテカる

**正体**: `translucent skin` や `dewy skin` のような**肌の質感タグ**です。透明感を出そうとして入れると、腕や脚がぬらついて不気味になります。

**対策**: 透明感は肌のタグではなく、**照明のタグ**で出します。

```
backlighting, sunlight through window, light particles
```

---

## 出てくる絵が「なんか微妙」

**正体**: 素体モデル（Illustrious、NoobAIなど）を、そのまま使っています。

これらは**すっぴん**です。多くの人がCivitaiで見て憧れる絵は、素体を誰かがチューニングした「マージモデル」の出力です。素体は、自分で追加学習させるための土台と割り切ったほうがいいです。

**対策**: 見せる用の絵は、**人気マージモデル＋拡大（`--hires`）**を既定にしてください。作者の場合、素体で「正直微妙」だった評価が、この2つを変えただけで一発でひっくり返りました。

---

## 参考: 実測値（M4 Mac / メモリ32GB）

| 条件 | 所要時間 |
|---|---|
| 832×1216・26ステップ | **約2分/枚** |
| 832×1216・8ステップ（ラフ確認用） | 約20秒/枚 |
| モデル読み込み（単一ファイル） | 約27秒 |
| 100枚バッチ | **約3.5時間** |

拡大（`--hires`）を有効にすると、おおむね倍の時間がかかります。
ラフ検討は低ステップ、本番は26前後、という使い分けが実用的です。
